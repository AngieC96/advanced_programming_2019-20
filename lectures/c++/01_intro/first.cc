int main() {		// this function returns a zero. But to whom? To who invoked him, that is our shell (= a program to run other programs)
	return 0;	// you could also don't put it, because it is implied
}
//the compiler always starts to compile what is written in the main
